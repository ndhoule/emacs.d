;;; ndhoule-keybindings.el -- Custom keybindings

;;; Commentary:
;;
;; Change custom keybindings in here.

;;; Code:

;; Keybindings for easily increasing font size
(global-set-key (kbd "s-+") 'text-scale-increase)
(global-set-key (kbd "s-_") 'text-scale-decrease)

(provide 'ndhoule-keybindings)
;;; ndhoule-keybindings.el ends here
